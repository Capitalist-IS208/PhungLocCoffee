<UserControl x:Class="PhungLocCoffee_POS.Views.SuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#F5F6FA">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <materialDesign:Card Grid.Column="0" UniformCornerRadius="8" Padding="15" Background="White">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,15">
                    <TextBlock Text="QUẢN LÝ NHÀ CUNG CẤP" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                    <TextBox materialDesign:HintAssist.Hint="Tìm kiếm nhà cung cấp..." 
                             Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,10,0,0"/>
                </StackPanel>

                <DataGrid x:Name="dgSuppliers" AutoGenerateColumns="False" IsReadOnly="True" 
                          CanUserAddRows="False" SelectionMode="Single"
                          HeadersVisibility="Column" Background="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên NCC" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Điện thoại" Binding="{Binding Phone}" Width="120"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Column="2" UniformCornerRadius="8" Padding="20" Background="White">
            <StackPanel>
                <TextBlock Text="THÔNG TIN NCC" FontSize="16" FontWeight="Bold" Foreground= "#D7A16A" Margin="0,0,0,15"/>

                <TextBox x:Name="txtName" materialDesign:HintAssist.Hint="Tên nhà cung cấp" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,10"/>
                <TextBox x:Name="txtPhone" materialDesign:HintAssist.Hint="Số điện thoại" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,10"/>
                <TextBox x:Name="txtAddress" materialDesign:HintAssist.Hint="Địa chỉ" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,10" Height="80" AcceptsReturn="True"/>

                <Button Content="Lưu thông tin" Style="{StaticResource MaterialDesignFlatMidBgButton}" Background="#D7A16A" Foreground="White" Margin="0,20,0,0"/>
                <Button Content="Thêm mới" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#D7A16A" BorderBrush="#D7A16A" Margin="0,10,0,0"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
